/**
 * Archivo de barril (barrel file) para exportar hooks personalizados de React
 * desde el directorio actual.
 *
 * Permite una importación centralizada y mejora la organización del código,
 * facilitando el acceso a hooks personalizados desde un único punto.
 * 
 * Actualmente exporta:
 * - useLanguage: Hook para gestionar la selección y detección del idioma.
 */

export * from './useLanguage';
